import * as defaultParam from './defaultParam';
import * as organizationSelectWidth from './organizationSelectWidth';
import * as paginationOption from './paginationOption';

export const Table = {
  defaultParam,
  organizationSelectWidth,
  paginationOption,
};
